

function _Example(props: any) {
    // const [name, setName] = useState(props.name || '')

    // const onChange = useCallback((event: any) => {
    //     props.putData(props.id, event.target.value);
    //     props.emitter.trigger("process");
    //     setName(event.target.value)
    // }, [props])

    // useEffect(() => {
    //     props.putData(props.id, props.name);
    // }, [props])

    return (
        null
    );
}

export {
    _Example
}